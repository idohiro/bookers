<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>
        title
      </th>
      <th>
        body
      </th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
   <% @books.each do |book| %>
        <th><%= link_to book.title, book_path(book) %></th>
            <th><%= book.body %></th>
      <th><p><a href="/books/show">Show</a></p></th>
      <th><p><a href="/books/edit">Edit</a></p></th>
      <th><p><a href="/books/destory">Destory</a></p></th>
   <% end %>

    </div>

  </tbody>
</table>


<h2>new books</h2>
<%= form_with model:@book, local:true do |f| %>

  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

  <% end %>