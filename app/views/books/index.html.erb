<h1>Books</h1>

<table>

    <tr>
      <th>
        title
      </th>
      <th>
        body
      </th>





   <% @books.each do |book| %>
        <th><%=book.title%></th>
            <th><%= book.body %></th>
      <th><%= link_to 'Show', book_path(book) %></th>
      <th><%= link_to "Edit", book_path(book) %></th>
      <th><%= link_to "Destory", book_path(book) %></th>
   <% end %>

    </tr>
</table>


<div>
<h2>new books</h2>
<%= form_with model:@book, local:true do |f| %>

  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

  <% end %>
  </div>
